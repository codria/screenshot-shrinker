# Screenshot Shrinker 仕様書

## 1. 概要

Androidスクリーンショットを「複数選択 → 各画像トリミング・モザイク → 連結（2枚以上の場合） → 縮小 → JPG保存 → 結果プレビュー」の一連の流れで処理するモバイルアプリ。
高解像度スクリーンショットを取り回しやすいサイズに圧縮することと、複数のスクリーンショットを1枚にまとめることを目的とする。

## 2. 背景・目的

- 端末のスクリーンショットは解像度が高く、共有・保存・閲覧で容量が嵩む
- 既存アプリの組み合わせ（ギャラリー → トリミングアプリ → 圧縮アプリ）は手間がかかる
- 自分のワークフローに最適化した一気通貫アプリを自作する

## 3. ターゲットユーザー

開発者本人（自分用ツール）。Play Store公開は視野に入れるが当面は対象外。

## 4. 動作環境

| 項目 | 内容 |
|---|---|
| プラットフォーム | Android |
| 最低APIレベル | API 26 (Android 8.0) |
| 開発言語 | Kotlin |
| 開発環境 | Android Studio |
| UIフレームワーク | View System (XML) |

## 5. 機能要件

### 5.1 実装済み機能

#### F-01: 画像入力（複数選択対応）
- ギャラリーから画像を**複数選択可能**に読み込む
- `ActivityResultContracts.GetMultipleContents`（または`PickMultipleVisualMedia`）を使用
- 選択した画像を画面内のリストに表示し、後続のトリミング・連結のソースとする
- 1枚のみの場合は連結ステップをスキップして縮小・保存に進む
- 他アプリからの `ACTION_SEND` / `ACTION_SEND_MULTIPLE` で直接起動可能（F-09）
- 前回の品質・リサイズ・連結方向設定を `SharedPreferences` で保持（F-10）

#### F-02: 個別トリミング
- F-01のリスト各項目に「切り取り」ボタンを配置
- 各画像を個別に矩形範囲指定でトリミングできる
- ドラッグでハンドルを動かして範囲調整（4角 + 4辺ハンドル、内部ドラッグで全体移動）
- アスペクト比プリセット: 自由 / 1:1 / 4:3 / 16:9 から選択可能
- 画像端へのスナップ（drag時、閾値以内で吸着）
- 元/選択中サイズの常時表示
- ピンチで拡大 (1〜5倍)、ドラッグで移動。拡大状態でクロップ境界を操作可能
- Undo/Redo: drag開始 / アスペクト変更 / プリセット読込 で履歴push
- 内部表現: **XYWHメタデータ方式**（クロップ情報のみ保持、最終JPG保存時に1回だけ適用）
  - 中間PNG/JPG生成なし、品質劣化なし
- トリミングは任意（未トリミング画像はそのまま後続処理へ）

#### F-03: 縮小
- リサイズ倍率を指定。%系と固定幅系のプリセットを%値で混合ソートして表示
- プリセット (%系): 100% / 50% / 40% / 30% / 20%
- プリセット (固定幅系): 1080px / 768px（元の幅から計算される%値で%リストに差し込み配置）
- カスタム: 1〜100%の任意の値をスライダーで指定
- 高さは元のアスペクト比から自動計算
- 拡大は行わない（100%以下のみ）

#### F-04: JPG保存
- JPG形式で出力
- 品質（quality）を指定可能（既定: 80）
- `MediaStore` API経由で端末ギャラリーに保存
- 保存先: `Pictures/ScreenshotShrinker/`
- 設定画面では保存前に**予測ファイルサイズ**を表示（メモリ内で実際にJPEG圧縮を実行してbyte数を計測した正確値、設定変更で随時更新）

#### F-05: 保存通知
- 保存完了時に結果通知
- 保存先パス・ファイル名・最終ファイルサイズを表示
- 詳細な確認はF-07の結果プレビュー画面で行う

#### F-06: 画像連結（2枚以上選択時）
- 選択画像が2枚以上の場合、縦方向または横方向に連結する
- 連結方向: ユーザーが選択（縦 / 横）
- サイズ整合方針: **小さい方（短辺）に合わせる**
  - 縦連結: 各画像の幅を、最も狭い画像の幅にダウンスケール
  - 横連結: 各画像の高さを、最も低い画像の高さにダウンスケール
  - 拡大は行わない（画質劣化を避ける）
- 余白・仕切り線: なし（隙間ゼロで密着）
- 画像順序: F-01のリスト並び順に従う（並び替えはF-08で行う）

#### F-07: 保存結果プレビュー
- 保存完了後、結果画像をアプリ内で表示
- 表示項目: 画像本体、ファイル名、解像度、ファイルサイズ
- 画像はピンチ拡大・ダブルタップリセット・ドラッグ移動可能（保存結果のディテール確認用）
- ボタン:
  - 「ギャラリーで開く」（ACTION_VIEW）
  - 「共有」（ACTION_SEND）
  - 「破棄」: 設定画面に戻り、リサイズ・品質を変更して再保存できる
  - 「最初から」: 最初の画面に戻り、別の画像で処理を開始

#### F-08: 画像順序の入替
- F-01のリスト内で画像の並び順を変更できる
- 連結時の出力順序はこのリスト並びに従う
- 操作方法: 各行に「上へ」「下へ」ボタン

#### F-12: モザイク
- F-01のリスト各項目に「モザイク」ボタンを配置
- 各画像に対して複数のモザイク領域を独立して設定できる
- 矩形ドラッグで領域を追加、ハンドルドラッグで移動・リサイズ
- 粗さ（ブロックサイズ）調整可能
- Undo/Redo対応
- ピンチ拡大・ドラッグ移動でのズーム操作対応
- モザイクは任意（設定なしでもそのまま後続処理へ）

#### F-13: プリセット
- トリミング＋モザイク設定を3スロットに保存できる
- 保存: トリミング画面またはモザイク画面から現在の設定をスロットに保存
- 適用: 編集画像リスト画面からプリセットをワンタップで全画像に再適用
- 別の画像セットで同じレイアウトの処理を繰り返す用途に対応

### 5.2 将来候補機能

#### F-09: 共有メニュー対応 ✓ 実装済み
（F-01に統合記載）

#### F-10: 設定の記憶 ✓ 実装済み
（F-01に統合記載）

#### F-11: 連結時の余白・仕切り線
- 連結する画像間に余白（N px）を空ける
- 仕切り線色（白／黒／カスタム）を選択

### 5.3 将来検討機能

- WebP出力対応
- EXIF情報の保持／削除選択（写真扱い時のorientation補正含む）
- 画像回転・反転
- 出力ファイル名規則のカスタマイズ
- ダークモード対応

## 6. 非機能要件

| 項目 | 要件 |
|---|---|
| 起動速度 | コールドスタート2秒以内を目標 |
| 処理速度 | 4000×3000程度の画像で3秒以内に保存完了（単一画像時） |
| メモリ | OOM回避のため `inSampleSize` で読み込み時にダウンサンプル。複数画像同時保持時は処理済み元Bitmapを順次 `recycle()` してメモリ回収 |
| 権限 | 最小権限。Android 10+ではScoped Storageに準拠し画像権限不要 |

## 7. 画面構成

| 画面 | 役割 | 主な操作・設定 | 遷移先 |
|---|---|---|---|
| 編集画像リスト | 画像選択とプリセット管理の起点 | 画像追加・削除、各画像のトリミング / モザイク設定、プリセット保存・適用 | トリミング / モザイク / 出力設定 |
| トリミング | クロップ範囲の指定 | ハンドルドラッグ、アスペクト比ロック、端スナップ、Undo/Redo、プリセット | 編集画像リスト |
| モザイク | モザイク領域の配置 | 領域追加・削除・移動・リサイズ、粗さ調整、Undo/Redo、プリセット | 編集画像リスト |
| 出力設定 | 出力パラメーターの設定と確認 | 連結方向（縦 / 横）、リサイズ、JPEG 品質（推定ファイルサイズ表示） | 保存完了 |
| 保存完了 | 保存結果の確認 | ギャラリーで開く・共有・破棄・最初からやり直し | — |

## 8. 技術選定

### 8.1 トリミングUI

**自前実装（Canvas + MotionEvent）** を採用。

ライブラリ（uCrop / Android-Image-Cropper）を当初候補としていたが、モザイク領域との操作感統一・プリセット機能との連携のため自前実装が適切と判断。

### 8.2 UIフレームワーク

**View System (XML)** で実装。Jetpack Compose は将来移行候補。

### 8.3 画像処理

- `Bitmap` を中心に処理
- 読み込み: `BitmapFactory.decodeStream` + `inSampleSize`
- リサイズ: `Bitmap.createScaledBitmap`
- JPG出力: `Bitmap.compress(JPEG, quality, OutputStream)`
- 保存: `MediaStore.Images.Media.EXTERNAL_CONTENT_URI`
- モザイク: ブロック平均色で塗りつぶし（`Canvas` + `Paint`）

### 8.4 ウィジェット設計

- `ZoomableImageView`: ピンチ拡大・パン共通基底クラス。`viewToImageX/Y()` で座標変換、`startRectDrag/applyRectDrag()` で矩形内部ドラッグ共通化
- `CropView`: ZoomableImageView サブクラス。クロップハンドル描画・操作
- `MosaicView`: ZoomableImageView サブクラス。モザイク領域描画・操作
- `PreviewView`: ZoomableImageView サブクラス。読み取り専用プレビュー（ダブルタップリセット付き）

## 9. ファイル命名規則

出力ファイル名の既定:

```
単一画像:
  shrunk_<タイムスタンプ>.jpg
  例: shrunk_260505_193045.jpg

連結画像（2枚以上）:
  concat_<タイムスタンプ>.jpg
  例: concat_260505_193045.jpg
```

タイムスタンプ書式: `yyMMdd_HHmmss`（端末ローカル時刻）。

設計判断:
- 単一/連結とも独自タイムスタンプで命名し、形式を統一（短く、簡潔、保存時刻で並び順が分かる）
- 元ファイル名は使用しない（スクリーンショットの命名書式が端末・ROMで異なるため、自前タイムスタンプの方が予測可能）
- 2桁年（yyMMdd）でファイル名長を短縮（4桁年は冗長）

ファイル名衝突時の扱い: `MediaStore` API側で自動的に末尾に `(1)`, `(2)` 等を付与する挙動に任せる（独自対応はしない）。

## 10. プロジェクト構成

```
screenshot-shrinker/
├── app/
│   ├── src/main/
│   │   ├── java/com/codria/screenshotshrinker/
│   │   │   ├── MainActivity.kt          画像選択 + リスト表示 + プリセット適用
│   │   │   ├── CropActivity.kt          個別トリミング
│   │   │   ├── MosaicActivity.kt        モザイク領域設定
│   │   │   ├── SettingsActivity.kt      リサイズ・品質・連結方向（出力設定）
│   │   │   ├── ResultActivity.kt        保存結果プレビュー
│   │   │   ├── util/
│   │   │   │   ├── FormatUtils.kt       ファイルサイズ等の表示フォーマット
│   │   │   │   ├── ImageConcatenator.kt 縦/横連結（短辺合わせ）
│   │   │   │   ├── ImageLoader.kt       Bitmapロード（inSampleSize対応）
│   │   │   │   ├── ImageMosaicker.kt    モザイク適用（ブロック平均色）
│   │   │   │   ├── ImageResizer.kt      リサイズ計算・適用
│   │   │   │   ├── ImageSaver.kt        MediaStore JPG保存
│   │   │   │   ├── LongClickHelper.kt   長押し操作ユーティリティ
│   │   │   │   ├── PresetSlotManager.kt トリミング+モザイク設定の3スロット保存
│   │   │   │   └── TopToast.kt          通知表示ユーティリティ
│   │   │   └── widget/
│   │   │       ├── ZoomableImageView.kt ピンチ拡大・パン・座標変換の基底クラス
│   │   │       ├── CropView.kt          トリミングUI（ZoomableImageView継承）
│   │   │       ├── MosaicView.kt        モザイクUI（ZoomableImageView継承）
│   │   │       └── PreviewView.kt       読み取り専用プレビュー（ZoomableImageView継承）
│   │   ├── res/
│   │   │   ├── layout/
│   │   │   │   ├── activity_main.xml
│   │   │   │   ├── activity_crop.xml
│   │   │   │   ├── activity_mosaic.xml
│   │   │   │   ├── activity_settings.xml
│   │   │   │   ├── activity_result.xml
│   │   │   │   └── item_image_row.xml
│   │   │   └── values/
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
├── README.md
├── SPECIFICATION.md  ← 本ドキュメント
└── .gitignore
```

## 11. 開発ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 0 | 環境構築・GitHubリポジトリ作成 | 完了 |
| Phase 1 | 画像選択 → 単純JPG保存（疎通確認） | 完了 |
| Phase 2 | リサイズ＋JPG品質指定保存（単一画像、F-03/F-04） | 完了 |
| Phase 3 | 結果プレビュー画面（F-07） | 完了 |
| Phase 4-5 | 入力フロー刷新（複数選択 + リストUI + 順序入替）+ 画像連結機能（F-01/F-06/F-08） | 完了 |
| Phase 6 | 個別トリミング（アスペクト比プリセット、XYWHメタデータ方式、F-02） | 完了 |
| Phase 7 | 共有メニュー対応（F-09） | 完了 |
| Phase 8 | 設定記憶（F-10） | 完了 |
| Phase 9 | モザイク機能（F-12） | 完了 |
| Phase 10 | プリセット機能（F-13） | 完了 |
| Phase 11 | UI統一・リファクタ（PreviewView共通化、ドラッグoffset統一、画面タイトル追加） | 完了 |

## 12. 未確定事項

### 解決済み

| 事項 | 決定内容 |
|---|---|
| トリミングライブラリの選定 | 自前実装（Canvas + MotionEvent）で確定 |
| 順序入替UIの操作方式 | 上下ボタンで確定 |
| 設定画面のスタイル | 独立Activity（SettingsActivity）で確定 |
| 3枚以上選択時のUX | n枚対応で確定 |

### 未確定

- トリミングプリセットの拡張（位置プリセット: ステータスバー除外・ナビゲーションバー除外を追加するか）
- 連結時の余白・仕切り線（F-11）— v1.1で必要性を判断、当面は密着

---

最終更新: 2026-05-06（モザイク機能・プリセット機能・ウィジェット設計を追加、Phase 6-11完了に更新、解決済み未確定事項を整理、画面構成をテーブル化、§5再編）  
作者: codria
