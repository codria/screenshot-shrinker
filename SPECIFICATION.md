# Screenshot Shrinker 仕様書

## 1. 概要

Androidスクリーンショットを「複数選択 → 各画像トリミング → 連結（2枚以上の場合） → 縮小 → JPG保存 → 結果プレビュー」の一連の流れで処理するモバイルアプリ。
高解像度スクリーンショットを取り回しやすいサイズに圧縮することと、複数のスクリーンショットを1枚にまとめることを目的とする。

## 2. 背景・目的

- 端末のスクリーンショットは解像度が高く、共有・保存・閲覧で容量が嵩む
- 既存アプリの組み合わせ（ギャラリー → トリミングアプリ → 圧縮アプリ）は手間がかかる
- 自分のワークフローに最適化した一気通貫アプリを自作する

## 3. ターゲットユーザー

開発者本人（自分用ツール）。Play Store公開は視野に入れるが当面は対象外。

## 4. 動作環境

| 項目 | 内容 |
|---|---|
| プラットフォーム | Android |
| 最低APIレベル | API 26 (Android 8.0) |
| 開発言語 | Kotlin |
| 開発環境 | Android Studio |
| UIフレームワーク | View System もしくは Jetpack Compose（後述） |

## 5. 機能要件

### 5.1 必須機能（MVP）

#### F-01: 画像入力（複数選択対応）
- ギャラリーから画像を**複数選択可能**に読み込む
- `ActivityResultContracts.GetMultipleContents`（または`PickMultipleVisualMedia`）を使用
- 選択した画像を画面内のリストに表示し、後続のトリミング・連結のソースとする
- 1枚のみの場合は連結ステップをスキップして縮小・保存に進む

#### F-02: 個別トリミング
- F-01のリスト各項目に「切り取り」ボタンを配置
- 各画像を個別に矩形範囲指定でトリミングできる
- ドラッグでハンドルを動かして範囲調整
- アスペクト比プリセット: 自由 / 1:1 / 4:3 / 16:9 / 9:16 から選択可能
- トリミングは任意（未トリミング画像はそのまま後続処理へ）

#### F-03: 縮小
- リサイズ倍率を指定。%系と固定幅系のプリセットを%値で混合ソートして表示
- プリセット (%系): 100% / 50% / 40% / 30% / 20%
- プリセット (固定幅系): 1080px / 768px（元の幅から計算される%値で%リストに差し込み配置）
- カスタム: 1〜100%の任意の値を入力（プレフィックスは「カスタム」）
- 高さは元のアスペクト比から自動計算
- ラベル形式（統一フォーマット「X% (W×Hpx)」、太字は固定幅プリセットのみ）:
  - %系: 通常太さで「X% (W×Hpx)」 例: `75% (3000×2250px)`
  - 固定幅系: 全体太字で「X% (W×Hpx)」 例: `**27% (1080×810px)**` ← 元4000×3000、Wが1080なので1080px固定と識別
  - カスタム: 通常太さで「カスタム」
- 表示制御:
  - 固定幅系の計算結果Wが標準%プリセットの結果Wと一致する場合、固定幅は非表示（重複回避）
  - 元の幅より大きい固定幅は非表示（拡大しないため意味なし）
- 拡大は行わない（100%以下のみ）

#### F-04: JPG保存
- JPG形式で出力
- 品質（quality）を指定可能（既定: 80）
- `MediaStore` API経由で端末ギャラリーに保存
- 保存先: `Pictures/ScreenshotShrinker/`
- 設定画面では保存前に**予測ファイルサイズ**を表示（メモリ内で実際にJPEG圧縮を実行してbyte数を計測した正確値、設定変更で随時更新）

#### F-05: 保存通知
- 保存完了時にスナックバー等で結果通知
- 保存先パス・ファイル名・最終ファイルサイズを表示
- 詳細な確認はF-07の結果プレビュー画面で行う

#### F-06: 画像連結（2枚以上選択時）
- 選択画像が2枚以上の場合、縦方向または横方向に連結する
- 連結方向: ユーザーが選択（縦 / 横）
- サイズ整合方針: **小さい方（短辺）に合わせる**
  - 縦連結: 各画像の幅を、最も狭い画像の幅にダウンスケール
  - 横連結: 各画像の高さを、最も低い画像の高さにダウンスケール
  - 拡大は行わない（画質劣化を避ける）
- 余白・仕切り線: なし（隙間ゼロで密着）※v1.1以降で検討
- 画像順序: F-01のリスト並び順に従う（並び替えはF-08で行う）

#### F-07: 保存結果プレビュー
- 保存完了後、結果画像をアプリ内で表示
- 表示項目: 画像本体、ファイル名、解像度、ファイルサイズ
- 画像はピンチ拡大・ダブルタップ・ドラッグ移動可能（保存結果のディテール確認用）
- ボタン:
  - 「ギャラリーで開く」（ACTION_VIEW）
  - 「共有」（ACTION_SEND）
  - 「戻る」: 同じ元画像で設定画面に戻り、リサイズ・品質を変更して再保存できる（保存結果が大きすぎ等の調整用途）
  - 「最初から」: 最初の画面に戻り、別の画像で処理を開始

#### F-08: 画像順序の入替
- F-01のリスト内で画像の並び順を変更できる
- 連結時の出力順序はこのリスト並びに従う
- 操作方法: 各行に「上へ」「下へ」ボタン、もしくはロングタップ＋ドラッグ（実装方式は未確定、§12参照）

### 5.2 推奨機能（v1.1以降）

#### F-09: 共有メニュー対応
- 他アプリの「共有」から本アプリを呼び出せる
- `AndroidManifest.xml` に `ACTION_SEND` / `image/*` のintent-filter登録
- `ACTION_SEND_MULTIPLE` も登録し、複数画像同時共有に対応

#### F-10: 設定の記憶
- 前回使用した品質・リサイズ・連結方向設定を `SharedPreferences` で保持

#### F-11: 連結時の余白・仕切り線
- 連結する画像間に余白（N px）を空ける
- 仕切り線色（白／黒／カスタム）を選択

### 5.3 将来検討機能

- WebP出力対応
- EXIF情報の保持／削除選択（写真扱い時のorientation補正含む）
- 画像回転・反転
- 出力ファイル名規則のカスタマイズ
- ダークモード対応
- 3枚以上の連結UX検証（技術的には可能、UI設計が要検討）

## 6. 非機能要件

| 項目 | 要件 |
|---|---|
| 起動速度 | コールドスタート2秒以内を目標 |
| 処理速度 | 4000×3000程度の画像で3秒以内に保存完了（単一画像時） |
| メモリ | OOM回避のため `inSampleSize` で読み込み時にダウンサンプル。複数画像同時保持時は処理済み元Bitmapを順次 `recycle()` してメモリ回収 |
| 権限 | 最小権限。Android 10+ではScoped Storageに準拠し画像権限不要 |

## 7. 画面構成

```
[起動画面（兼 画像リスト）]
  - 「画像を選択」ボタン（複数選択可）
  - 選択した画像のリスト
    各行: ファイル名（任意でサムネイル）+「切り取り」ボタン + 順序入替操作
  - 「次へ」ボタン（1枚以上選択時に有効）
   ↓ 「切り取り」ボタン（任意）
[トリミング画面]
  - 画像表示エリア
  - 矩形選択ハンドル
  - アスペクト比プリセット選択（自由 / 1:1 / 4:3 / 16:9 / 9:16）
  - 「OK」「キャンセル」ボタン
   ↓ OKで起動画面に戻る
   
   ↓ 「次へ」ボタン
[縮小・連結・品質設定画面]
  - リサイズ方式選択（プリセット / カスタム）
  - カスタム入力欄(120dp固定)は常時表示。横にリアルタイム算出した解像度（W×Hpx）を併記
  - 入力欄に値を入れると自動的にカスタムが選択される（プリセットからの切替操作不要）
  - 品質スライダー（1〜100）
  - 予測ファイルサイズ表示（実圧縮で算出、設定変更時にdebounce後更新）
  - 連結方向選択: 縦 / 横（2枚以上選択時のみ表示）
  - 「保存」ボタン
   ↓
[結果プレビュー画面]
  - 保存した画像表示
  - ファイル名・解像度・ファイルサイズ表示
  - 「ギャラリーで開く」「共有」「もう一度」ボタン
```

簡略化のため、画面間の遷移はActivity単位とするか単一Activity内でのstate変更とするかは実装時に判断。

## 8. 技術選定

### 8.1 トリミングUI

以下から選択。MVPでは**ライブラリ利用**を推奨。

| 選択肢 | メリット | デメリット |
|---|---|---|
| ライブラリ（uCrop / Android-Image-Cropper） | 実装早い・安定 | カスタマイズ性に制限 |
| 自前実装（Canvas + MotionEvent） | 自由度高い | 実装コスト大 |

### 8.2 UIフレームワーク

| 選択肢 | 備考 |
|---|---|
| View System (XML) | 既存ライブラリとの互換性高い・情報量多い |
| Jetpack Compose | モダン・宣言的・将来性 |

学習コストとライブラリ互換性を考慮し、MVPでは **View System** で着手予定。

### 8.3 画像処理

- `Bitmap` を中心に処理
- 読み込み: `BitmapFactory.decodeStream` + `inSampleSize`
- リサイズ: `Bitmap.createScaledBitmap`
- JPG出力: `Bitmap.compress(JPEG, quality, OutputStream)`
- 保存: `MediaStore.Images.Media.EXTERNAL_CONTENT_URI`

## 9. ファイル命名規則

出力ファイル名の既定:

```
単一画像:
  shrunk_<タイムスタンプ>.jpg
  例: shrunk_260505_193045.jpg

連結画像（2枚以上）:
  concat_<タイムスタンプ>.jpg
  例: concat_260505_193045.jpg
```

タイムスタンプ書式: `yyMMdd_HHmmss`（端末ローカル時刻）。

設計判断:
- 単一/連結とも独自タイムスタンプで命名し、形式を統一（短く、簡潔、保存時刻で並び順が分かる）
- 元ファイル名は使用しない（スクリーンショットの命名書式が端末・ROMで異なるため、自前タイムスタンプの方が予測可能）
- 2桁年（yyMMdd）でファイル名長を短縮（4桁年は冗長）

ファイル名衝突時の扱い: `MediaStore` API側で自動的に末尾に `(1)`, `(2)` 等を付与する挙動に任せる（独自対応はしない）。同秒に2回保存した場合のみ発生し得る。

## 10. プロジェクト構成（予定）

```
screenshot-shrinker/
├── app/
│   ├── src/main/
│   │   ├── java/com/codria/screenshotshrinker/
│   │   │   ├── MainActivity.kt          画像選択 + リスト表示 + 「次へ」
│   │   │   ├── CropActivity.kt          個別トリミング
│   │   │   ├── SettingsActivity.kt      リサイズ・品質・連結方向
│   │   │   ├── ResultActivity.kt        保存結果プレビュー
│   │   │   └── util/
│   │   │       ├── ImageLoader.kt       Bitmapロード（inSampleSize対応）
│   │   │       ├── ImageResizer.kt      リサイズ計算・適用
│   │   │       ├── ImageConcatenator.kt 縦/横連結（短辺合わせ）
│   │   │       └── ImageSaver.kt        MediaStore JPG保存
│   │   ├── res/
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
├── README.md
├── SPECIFICATION.md  ← 本ドキュメント
├── HANDOVER.md
└── .gitignore
```

## 11. 開発ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 0 | 環境構築・GitHubリポジトリ作成 | 完了 |
| Phase 1 | 画像選択 → 単純JPG保存（疎通確認） | 完了 |
| Phase 2 | リサイズ＋JPG品質指定保存（単一画像、F-03/F-04） | 進行中 |
| Phase 3 | 結果プレビュー画面（F-07） | 未着手 |
| Phase 4 | 入力フロー刷新（複数選択 + リストUI + 順序入替、F-01/F-08） | 未着手 |
| Phase 5 | 画像連結機能（F-06） | 未着手 |
| Phase 6 | 個別トリミング（アスペクト比プリセット含む、F-02） | 未着手 |
| Phase 7 | 共有メニュー対応（F-09） | 未着手 |
| Phase 8 | 設定記憶（F-10） | 未着手 |

順序の理由: 段階的に複雑度を上げて各段階で動作確認できるよう構成。Phase 2-3は単一画像フローの強化、Phase 4で入力フロー刷新、Phase 5-6でリストを起点とした複数画像処理、Phase 7-8で残りの推奨機能。

## 12. 未確定事項

- トリミングライブラリの最終選定（uCrop / Android-Image-Cropper / 自前）— Phase 6で判断
- 順序入替UIの操作方式（上下ボタン / ロングタップ＋ドラッグ / 両方提供）— Phase 4で判断
- トリミングプリセットの拡張（位置プリセット: ステータスバー除外・ナビゲーションバー除外を追加するか）— Phase 6で判断
- 連結時の余白・仕切り線（F-11）— v1.1で必要性を判断、当面は密着
- 設定画面のスタイル（独立Activity / BottomSheet / state変更）— Phase 2着手時に判断
- 3枚以上選択時のUX（n枚連結を許すか、2枚に制限するか）— Phase 5前に決定

---

最終更新: 2026-05-05（複数画像入力・連結・結果プレビュー・順序入替・トリミングプリセットを追加、ファイル命名規則を簡素化、リサイズUIを%統一+解像度併記化、F-07にやりなおしボタン追加、フェーズ計画再編）  
作者: codria
